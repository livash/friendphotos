App.Photo = function(params) {
  this.title = params.title;
  this.url = params.url;
};

App.Photo.fetch = function () {

};

App.Photo.find = function (id) {
  _(App.Photo._all).
}


we'll be working on it tomorrow anyways'